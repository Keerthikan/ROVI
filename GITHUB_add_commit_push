Jeg ved ikke lige hvordan du interagere med github, men hvis du bruger terminalen som jeg gør lige nu for at kunne pushe (add), commit, push. 

Så f.eks. en fil 

git status (fortæller om der er foretaget nogen form for ændringer)
git add * (tilføjer alle ændringer) -  hvis det er nødvendigt
---- Prøv at leg med det lidt selv hvis det er ---  HAr lige selv bøvlet med 2x samme fil. :(
git commit -a -m "Some message"
git push -u origin master 
